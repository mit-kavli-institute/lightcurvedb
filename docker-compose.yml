version: '3.9'
services:
  db:
    image: postgres:14.4
    env_file:
      - .env
  test_runner:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
    env_file:
      - .env
    volumes:
      - .:/testing
      - /tmp:/tmp
